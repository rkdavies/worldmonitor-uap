// @ts-nocheck
// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/uap/v1/service.proto

export interface ListUapSightingsRequest {
  neLat: number;
  neLon: number;
  swLat: number;
  swLon: number;
  sinceDate: number;
  source: string;
  minCredibility: number;
  pageSize: number;
}

export interface ListUapSightingsResponse {
  sightings: UapSighting[];
}

export interface UapSighting {
  id: string;
  lat: number;
  lon: number;
  timestamp: number;
  source: string;
  shape: string;
  description: string;
  credibilityScore: number;
}

export interface ListUapInstitutionalReportsRequest {
  org: string;
  limit: number;
}

export interface ListUapInstitutionalReportsResponse {
  reports: UapInstitutionalReport[];
}

export interface UapInstitutionalReport {
  id: string;
  org: string;
  title: string;
  url: string;
  publishedAt: number;
  reportType: string;
}

export interface ListUapSensorStationsRequest {
}

export interface ListUapSensorStationsResponse {
  stations: UapSensorStation[];
}

export interface UapSensorStation {
  id: string;
  name: string;
  lat: number;
  lon: number;
  type: string;
  status: string;
}

export interface GetAaiScoresRequest {
  region: string;
}

export interface GetAaiScoresResponse {
  scores: AaiScore[];
}

export interface AaiScore {
  regionId: string;
  score: number;
  level: string;
  trend: string;
  sightingDensity: number;
  institutionalActivity: number;
  lastUpdated: number;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface UapServiceHandler {
  listUapSightings(ctx: ServerContext, req: ListUapSightingsRequest): Promise<ListUapSightingsResponse>;
  listUapInstitutionalReports(ctx: ServerContext, req: ListUapInstitutionalReportsRequest): Promise<ListUapInstitutionalReportsResponse>;
  listUapSensorStations(ctx: ServerContext, req: ListUapSensorStationsRequest): Promise<ListUapSensorStationsResponse>;
  getAaiScores(ctx: ServerContext, req: GetAaiScoresRequest): Promise<GetAaiScoresResponse>;
}

export function createUapServiceRoutes(
  handler: UapServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/uap/v1/list-uap-sightings",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListUapSightingsRequest = {
            neLat: Number(params.get("ne_lat") ?? "0"),
            neLon: Number(params.get("ne_lon") ?? "0"),
            swLat: Number(params.get("sw_lat") ?? "0"),
            swLon: Number(params.get("sw_lon") ?? "0"),
            sinceDate: Number(params.get("since_date") ?? "0"),
            source: params.get("source") ?? "",
            minCredibility: Number(params.get("min_credibility") ?? "0"),
            pageSize: Number(params.get("page_size") ?? "0"),
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listUapSightings", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listUapSightings(ctx, body);
          return new Response(JSON.stringify(result as ListUapSightingsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/uap/v1/list-uap-institutional-reports",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListUapInstitutionalReportsRequest = {
            org: params.get("org") ?? "",
            limit: Number(params.get("limit") ?? "0"),
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listUapInstitutionalReports", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listUapInstitutionalReports(ctx, body);
          return new Response(JSON.stringify(result as ListUapInstitutionalReportsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/uap/v1/list-uap-sensor-stations",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = {} as ListUapSensorStationsRequest;

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listUapSensorStations(ctx, body);
          return new Response(JSON.stringify(result as ListUapSensorStationsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/uap/v1/get-aai-scores",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: GetAaiScoresRequest = {
            region: params.get("region") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getAaiScores", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getAaiScores(ctx, body);
          return new Response(JSON.stringify(result as GetAaiScoresResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}


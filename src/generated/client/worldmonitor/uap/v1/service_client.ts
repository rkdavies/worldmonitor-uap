// @ts-nocheck
// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/uap/v1/service.proto

export interface ListUapSightingsRequest {
  neLat: number;
  neLon: number;
  swLat: number;
  swLon: number;
  sinceDate: number;
  source: string;
  minCredibility: number;
  pageSize: number;
}

export interface ListUapSightingsResponse {
  sightings: UapSighting[];
}

export interface UapSighting {
  id: string;
  lat: number;
  lon: number;
  timestamp: number;
  source: string;
  shape: string;
  description: string;
  credibilityScore: number;
}

export interface ListUapInstitutionalReportsRequest {
  org: string;
  limit: number;
}

export interface ListUapInstitutionalReportsResponse {
  reports: UapInstitutionalReport[];
}

export interface UapInstitutionalReport {
  id: string;
  org: string;
  title: string;
  url: string;
  publishedAt: number;
  reportType: string;
}

export interface ListUapSensorStationsRequest {
}

export interface ListUapSensorStationsResponse {
  stations: UapSensorStation[];
}

export interface UapSensorStation {
  id: string;
  name: string;
  lat: number;
  lon: number;
  type: string;
  status: string;
}

export interface GetAaiScoresRequest {
  region: string;
}

export interface GetAaiScoresResponse {
  scores: AaiScore[];
}

export interface AaiScore {
  regionId: string;
  score: number;
  level: string;
  trend: string;
  sightingDensity: number;
  institutionalActivity: number;
  lastUpdated: number;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface UapServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface UapServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class UapServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: UapServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async listUapSightings(req: ListUapSightingsRequest, options?: UapServiceCallOptions): Promise<ListUapSightingsResponse> {
    let path = "/api/uap/v1/list-uap-sightings";
    const params = new URLSearchParams();
    if (req.neLat != null && req.neLat !== 0) params.set("ne_lat", String(req.neLat));
    if (req.neLon != null && req.neLon !== 0) params.set("ne_lon", String(req.neLon));
    if (req.swLat != null && req.swLat !== 0) params.set("sw_lat", String(req.swLat));
    if (req.swLon != null && req.swLon !== 0) params.set("sw_lon", String(req.swLon));
    if (req.sinceDate != null && req.sinceDate !== 0) params.set("since_date", String(req.sinceDate));
    if (req.source != null && req.source !== "") params.set("source", String(req.source));
    if (req.minCredibility != null && req.minCredibility !== 0) params.set("min_credibility", String(req.minCredibility));
    if (req.pageSize != null && req.pageSize !== 0) params.set("page_size", String(req.pageSize));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListUapSightingsResponse;
  }

  async listUapInstitutionalReports(req: ListUapInstitutionalReportsRequest, options?: UapServiceCallOptions): Promise<ListUapInstitutionalReportsResponse> {
    let path = "/api/uap/v1/list-uap-institutional-reports";
    const params = new URLSearchParams();
    if (req.org != null && req.org !== "") params.set("org", String(req.org));
    if (req.limit != null && req.limit !== 0) params.set("limit", String(req.limit));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListUapInstitutionalReportsResponse;
  }

  async listUapSensorStations(req: ListUapSensorStationsRequest, options?: UapServiceCallOptions): Promise<ListUapSensorStationsResponse> {
    let path = "/api/uap/v1/list-uap-sensor-stations";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListUapSensorStationsResponse;
  }

  async getAaiScores(req: GetAaiScoresRequest, options?: UapServiceCallOptions): Promise<GetAaiScoresResponse> {
    let path = "/api/uap/v1/get-aai-scores";
    const params = new URLSearchParams();
    if (req.region != null && req.region !== "") params.set("region", String(req.region));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetAaiScoresResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}

